import { ToNumberDirective } from './to-number.directive';

describe('ToNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ToNumberDirective();
    expect(directive).toBeTruthy();
  });
});
